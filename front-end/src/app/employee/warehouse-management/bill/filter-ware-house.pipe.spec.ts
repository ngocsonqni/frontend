import { FilterWareHousePipe } from './filter-ware-house.pipe';

describe('FilterWareHousePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWareHousePipe();
    expect(pipe).toBeTruthy();
  });
});
